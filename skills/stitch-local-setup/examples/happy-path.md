# stitch-local-setup：正常路径示例

## 请求

用户给出明确目标、输入、交付格式与验收条件，希望使用 `stitch-local-setup` 完成一次有界任务。

## 执行

1. 识别触发条件与范围。
2. 检查本地工具、配置位置、权限和当前认证状态。
3. 展示计划和需要的授权点。
4. 通过受支持的交互入口完成最小配置，并立即清理敏感输入。
5. 只报告可用性、身份范围和脱敏错误，不输出凭据内容。

## 期望输出

返回 `COMPLETED`、真实证据与产物位置；任何未执行检查单独列在 `skipped` 中。
