# stitch-delete-project：正常路径示例

## 请求

用户给出明确目标、输入、交付格式与验收条件，希望使用 `stitch-delete-project` 完成一次有界任务。

## 执行

1. 识别触发条件与范围。
2. 列出精确对象、依赖关系、可恢复性和预期影响。
3. 展示计划和需要的授权点。
4. 仅对获批对象执行一次操作，不自动重试。
5. 返回删除对象、时间、结果和恢复路径（如存在）。

## 期望输出

返回 `COMPLETED`、真实证据与产物位置；任何未执行检查单独列在 `skipped` 中。
