# stitch-mcp-create-project：正常路径示例

## 请求

用户给出明确目标、输入、交付格式与验收条件，希望使用 `stitch-mcp-create-project` 完成一次有界任务。

## 执行

1. 识别触发条件与范围。
2. 校验输入、模型/工具能力、输出路径、预算上限和审批状态。
3. 展示计划和需要的授权点。
4. 按一次批准执行并记录请求标识；模糊结果先查询而不是重提。
5. 验证产物存在性、格式、哈希/标识、成本状态和质量门禁。

## 期望输出

返回 `COMPLETED`、真实证据与产物位置；任何未执行检查单独列在 `skipped` 中。
