# stitch-mcp-list-screens：正常路径示例

## 请求

用户给出明确目标、输入、交付格式与验收条件，希望使用 `stitch-mcp-list-screens` 完成一次有界任务。

## 执行

1. 识别触发条件与范围。
2. 确认查询范围、身份上下文、分页上限和输出字段。
3. 展示计划和需要的授权点。
4. 执行有界只读查询并保留来源标识。
5. 返回资源标识、查询条件、分页状态和未验证项。

## 期望输出

返回 `COMPLETED`、真实证据与产物位置；任何未执行检查单独列在 `skipped` 中。
